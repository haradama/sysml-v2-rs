[package]
name = "sysmlv2-rust"
description = "The Rust side of a SysML model, both ways: an existing Rust API surface imported as a SysML package, and Rust generated from a resolved model"
# its own, now that it has one: the preamble it generates is written
# the way `rustfmt` writes it, so a generated file comes back from
# `cargo fmt` unchanged
version = "0.1.1"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
# its own, not the workspace one: `readme` inherited from
# `workspace.package` resolves against the workspace root, so every
# crate published carried the whole project's README
readme = "README.md"
keywords.workspace = true
categories.workspace = true

[lib]
name = "sysml_rust"

[dependencies]
sysmlv2-model.workspace = true
sysmlv2-syntax.workspace = true
serde_json = "1"

[dev-dependencies]
sysmlv2-semantics.workspace = true

[lints]
workspace = true
